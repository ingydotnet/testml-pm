source_testml_dir: ../../testml-tml
local_testml_dir: ./testml
test_file_template: |
  use TestML;
  use t::Bridge;

  TestML::Lite->new(
      testml => '[% file %]',
      bridge => 't::Bridge',
  )->run;
test_file_dir: lite
include_testml_files:
- arguments.tml
- basic.tml
- exceptions.tml
- semicolons.tml
