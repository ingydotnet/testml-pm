source: ../../testml-tml
target: testml
template: |
  use TestML;
  TestML->run(
      bridge => 't::Bridge',
      testml => '[% file %]',
  );
skip:
- comments.tml
- data.tml
- external1.tml
- external2.tml
- syntax.tml
- topic.tml
