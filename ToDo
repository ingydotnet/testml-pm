== To Do List

+ Rename %Tests to %Plan
+ Move the Builder to Document
+ Create TestML::Runner::TAP
  + Inherit from TestML::Runner
+ Fix the unquoted string regexp
+ use TestML::Runner::TAP -run, -bridge => 'TestMLTestBridge';
  + for tests with inline data
- Build Tests object from parse events
- Parse Data Section
- Write the evaluation runtime
- Write the runtime spec
- Get better parser errors

== Ideas

- write a 'testml' command line tool
